import { SafeAreaView, Text } from 'react-native';

function Teams() {
  return (
    <SafeAreaView>
      <SafeAreaView>
        <Text>TEAMS</Text>
      </SafeAreaView>
    </SafeAreaView>
  );
}

export default Teams;
